import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: To disable Turbopack in Next.js 16, use environment variable:
  // Set TURBOPACK=0 before running 'next dev'
  // Or run: npx next dev (without --turbo flag)
};

export default nextConfig;
